<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
        B2B ENTERPRISE APPLICATION
        Bu uygulama, müşterilerin kendi backend sunucularına bağlanır.
        Her müşterinin backend URL'i farklıdır ve uygulama içinden dinamik olarak ayarlanır.
        Bazı müşteriler HTTP, bazıları HTTPS kullanabilir.
        Bu nedenle cleartext traffic'e izin verilmiştir.
        
        NOT: Kullanıcılar Settings sayfasından kendi backend URL'lerini girerler.
    -->
    
    <!-- 
        B2B kullanımı için HTTP'ye izin ver 
        Müşteri backend sunucuları HTTP kullanıyor olabilir
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- 
        GÜVENLİK NOTU:
        - Bu uygulama B2B (Business-to-Business) uygulamasıdır
        - Her müşteri kendi backend sunucusunu kullanır
        - Backend URL'leri dinamik olarak Settings'ten girilir
        - HTTPS kullanımı önerilir ancak zorunlu değildir (müşteri tercihine bağlı)
        - Kullanıcılara HTTPS kullanmaları önerilmelidir
    -->
</network-security-config>
